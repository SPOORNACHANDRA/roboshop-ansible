- name: install mysql client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: load schema
  ansible.builtin.shell: mysql -h {{ lookup('amazon.aws.aws_ssm', '{{component}}.{{env}}.DB_HOST', region='us-east-1') }} -uroot -pRoboShop@1 < /app/schema/{{component}}.sql